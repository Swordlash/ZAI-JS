{
  "name": "zai-js",
  "version": "1.0.0",
  "description": "",
  "directories": {
    "src": "src",
    "test": "test"
  },
  "scripts": {
    "test": "npx spago test",
    "install": "npx spago install",
    "prepare": "mkdir -p dist && rm -rf dist/*",
    "bundle": "npx spago build && npx spago bundle-app && npm run prepare && npx parcel build index.html",
    "serve": "http-server dist/",
    "start": "npm run install && npm run bundle && npm run serve"
  },
  "author": "Mateusz Goślinowski",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.0.0",
    "parcel": "^2.0.1",
    "purescript": "^0.14.5",
    "spago": "^0.20.3"
  },
  "dependencies": {
    "big-integer": "^1.6.51",
    "purescript-language-server": "^0.15.8"
  }
}
